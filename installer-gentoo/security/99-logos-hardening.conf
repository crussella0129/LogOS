# LogOS kernel hardening — /etc/sysctl.d/99-logos-hardening.conf

# Hide kernel pointers from unprivileged users
kernel.kptr_restrict = 2

# Restrict dmesg to root
kernel.dmesg_restrict = 1

# Restrict perf_event access
kernel.perf_event_paranoid = 3

# Disable magic SysRq key (except for sync+reboot in Gael watchdog)
kernel.sysrq = 0

# Disable unprivileged BPF
kernel.unprivileged_bpf_disabled = 1

# Reverse-path filtering (anti-spoofing)
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1

# Reject ICMP redirects
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0

# Respond to pings (useful for network diagnostics)
net.ipv4.icmp_echo_ignore_all = 0

# SYN flood protection
net.ipv4.tcp_syncookies = 1

# TIME-WAIT assassination protection
net.ipv4.tcp_rfc1337 = 1

# IPv6 redirect rejection
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.default.accept_redirects = 0
